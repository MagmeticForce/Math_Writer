package contents;

import javafx.scene.image.Image;
import javafx.scene.image.ImageView;

public class Program_Object {
    
    boolean exists = true;
    /*
    //these are unneccessary because you can use the "getLayoutX()" and "getLayoutY()" functions
    double[] x = {0};
    double[] y = {0}; //stored in an array so that value modifications are reflected outside of functions
    */

}
